import {Course} from "@/components/Course.tsx";
import type {FC} from "react";

export const CryptoCourse: FC = () => {
  return (
    <Course
      heading="Crypto"
      description="Cryp"
      images={[]}
    />
  )
}