import type {FC} from "react";
import {Course} from "@/components/Course.tsx";

export const TypescriptCourse: FC = () => {
  return (
    <Course
      heading="Typescript"
      description=""
      images={[]} />
  )
}